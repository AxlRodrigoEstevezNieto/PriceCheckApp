# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PriceCheckApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PriceCheckApp
  pod "SwiftSpinner"
  pod 'BarcodeScanner'

  target 'PriceCheckAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PriceCheckAppUITests' do
    # Pods for testing
  end

end
